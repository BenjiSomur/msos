#!/bin/bash
read -p "Enter the name of the machine: " filename
eval "$(conda shell.bash hook)"
conda activate general
#for (( i = 4; i <= 5; i++)); do
    for ((j = 1; j <=31; j++)) do
        python3 main.py 7 $j $filename
        echo "Execution $j of instance 7 finished"
    done
#done

echo "Computation complete :)"
conda deactivate
